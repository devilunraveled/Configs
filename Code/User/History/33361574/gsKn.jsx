import { Box } from "@mui/material";
import { styled } from "@mui/system";

const FlexIt = styled(Box)({
    display : "flex",
    justifyContent : "space-between",
    alignItems : "center",
});

// *This is basically the Flex component that
// *we will use repeatedly. 

export default FlexIt;