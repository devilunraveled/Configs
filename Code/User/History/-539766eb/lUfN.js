import Post from "../models/Posts.js";

/*Create Controls*/
export const createPost = async ( req, res ) => {
    try{
        const { userId, postContent, postedSubGreddiit, tags } = req.params;
    }
}